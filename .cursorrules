Ce projet est une application de gestion de tâches (TaskManager) construite avec
Next.js (App Router) et Tailwind CSS.

Conventions du projet :
- Composants fonctionnels React avec hooks
- Tailwind CSS pour tout le style (pas de CSS-in-JS, pas de fichiers .css)
- "use client" obligatoire pour tout composant avec useState, useEffect ou
événements
- Un composant par fichier dans src/components/
- Pages dans src/app/ (App Router Next.js)
- Noms de variables et fonctions en anglais
- Accessibilité : labels, aria-attributes, navigation clavier sur tous les
éléments interactifs

Composants existants :
- AddTaskForm : formulaire d'ajout de tâche
- Dashboard : section "Tableau de bord" affichée sur la page tasks
- TaskStats : statistiques des tâches (total, complétées, actives, progression)
- ProgressBar : barre de progression accessible (role="progressbar", aria-*)
- TaskItem : affiche une tâche individuelle
- TaskList : affiche la liste des tâches
- SearchBar : barre de recherche
- FilterBar : filtres par statut (toutes/actives/complétées)
- Header : en-tête global
- Footer : pied de page global
- ContactCard : carte d'information de contact

Pages existantes (App Router) :
- src/app/page.js : accueil
- src/app/tasks/page.js : gestion des tâches (inclut Dashboard au-dessus de SearchBar)
- src/app/contact/page.js : page contact
- src/app/contact/ContactPageClient.js : logique client de la page contact
- src/app/profile/page.js : page profil

Format des données de tâche (référence TaskList/tasks page) :
- Objet avec au minimum : id, title, description, date, priority, completed
- completed attendu comme booléen (true/false)

Quand tu génères du code :
- Suis les conventions ci-dessus
- Vérifie l'accessibilité
- Gère les cas limites (liste vide, valeurs nulles)